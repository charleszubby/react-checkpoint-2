import React from "react";
import PlayersList from "./components/PlayersList";

const App = () => {
  return (
    <div style={{ backgroundColor: "grey" }}>
      <PlayersList />
    </div>
  );
};

export default App;
